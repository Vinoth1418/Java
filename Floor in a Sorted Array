class Solution {

    static int findFloor(int[] arr, int k) {
        int b=-1;
        for(int i=0;i<arr.length;i++){
            if(k>=arr[i]){
                b=i;
            }
        }
        if(b>=0){
            return b;
        }
        else{
            return -1;
        }
    }
}
